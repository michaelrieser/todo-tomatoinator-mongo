class TasksCtrl {
    constructor($state) {
        'ngInject';
        // The profile for this page, resolved by UI Router
        // this.tasks = tasks;
        // this.tasksState = $state.current.name.replace('app.tasks', '');
    }
}

export default TasksCtrl;
